public int[] pre4(int[] nums) {
  for (int i = 0; i < nums.length; i++) {
    if (nums[i] == 4) {
      int[] result = new int[i];
      for (int j = 0; j < i; j++) {
        result[j] = nums[j];
      }
      return result;
    }
  }
  return nums; // won't happen per problem spec
}
