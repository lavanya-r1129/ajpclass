public String minCat(String a, String b) {
  int minLen=Math.min(a.length(), b.length());
  
  String aEnd = a.substring(a.length()-minLen);
  String bEnd = b.substring(b.length()-minLen);
  
  return aEnd + bEnd;
}
