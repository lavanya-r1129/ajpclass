public int sum13(int[] nums) {
  int sum = 0;
  
  for (int i = 0; i < nums.length; i++) {
    if (nums[i] == 13) {
      i++; // skip the 13 AND the next number
      continue;
    }
    sum += nums[i];
  }
  
  return sum;
}
