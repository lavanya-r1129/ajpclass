public int[] squareUp(int n) {
  int[] result = new int[n * n];
  
  for (int i = 1; i <= n; i++) {
    // For each group i, fill the last i spots
    for (int j = 1; j <= i; j++) {
      result[i * n - j] = j;
    }
  }
  
  return result;
}
