public String mixString(String a, String b) {
  String result = "";
  int i = 0;
  
  while (i < a.length() && i < b.length()) {
    result += a.charAt(i);
    result += b.charAt(i);
    i++;
  }
  
  result += a.substring(i) + b.substring(i); // one of these is ""
  return result;
}
